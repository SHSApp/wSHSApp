@inject IReport rep

<Dropdown>
    <DropdownToggle Color="Color.Primary">
        Сформировать отчет
    </DropdownToggle>
    <DropdownMenu>
        <DropdownItem Clicked="@(() => { lRep?.Show(); })">@rep.Name</DropdownItem>
    </DropdownMenu>
</Dropdown>

<LetterReport Rep="rep" @ref="lRep" />

@code {
    LetterReport? lRep;
}
